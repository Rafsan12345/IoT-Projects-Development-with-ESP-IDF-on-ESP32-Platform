



set(COMPONENT_EMBED_TXTFILES "certs/cacert.pem" "certs/prvtkey.pem")
target_add_binary_data(${COMPONENT_TARGET} "certs/cacert.pem" TEXT)

target_add_binary_data(${COMPONENT_TARGET} "certs/prvtkey.pem" TEXT)





// SSL Command 
openssl req -newkey rsa:2048 -nodes -keyout prvtkey.pem -x509 -days 365 -out cacert.pem -subj "/CN=ESP32 HTTPS server example"